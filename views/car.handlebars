
<div class="text-center">
  <h2>{{user.name}}</h2>
  
  <p>{{description}}</p>
  <p>Created by {{user.name}} for sale {{model}}</p>
  <p>
    <span for="img" aria-label="money">💲</span>
    <span class="dollar-amount">{{format_amount price}}</span>
    needed
  </p>
  <p>
    <button class="btn btn-primary">Buy this Car!!!</button>
  </p>
</div>
